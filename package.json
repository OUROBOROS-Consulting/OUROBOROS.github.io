{
  "name": "ouroboros-consulting-website",
  "version": "1.0.0",
  "description": "OUROBOROS Consulting website",
  "scripts": {
    "install-design": "npm install",
    "dev": "bundle exec jekyll serve --livereload",
    "build": "bundle exec jekyll build"
  },
  "dependencies": {
    "@OUROBOROS-Consulting/agentic-design": "^1.0.0"
  }
}
